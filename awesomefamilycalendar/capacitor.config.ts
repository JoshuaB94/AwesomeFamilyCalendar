import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.awesomefamilycalendar.app',
  appName: 'awesomefamilycalendar',
  webDir: 'public',
  bundledWebRuntime: false
};

export default config;
